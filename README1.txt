Antes de inicializar la página de forma local

aplicar por consola:

rails assets:precompile

Luego:

rails s

Los archivos con nombre modelo_arreglado son lo mismo pero el formato .png se visualiza mejor.
